# Configuração comum. Cada nó ajusta node-id e porta HTTP via profile (n1/n2/n3).
spring.application.name=instituicao

# Nó padrão quando nenhum profile é ativado (equivale a rodar como n1).
instituicao.node-id=n1
server.port=8081

# Raft ligado por padrão: escritas passam pelo consenso (precisa de maioria de nós).
# Para desenvolver/testar o endpoint num único processo, use: --instituicao.raft.enabled=false
instituicao.raft.enabled=true

# Identificador desta instituição (usado ao registrar chaves no servidor de chaves).
instituicao.id=INST-0001

# Endereço do servidor de chaves via RMI. Em Docker/K8s aponte para o
# serviço correspondente (env CHAVES_HOST / CHAVES_PORT).
chaves.host=127.0.0.1
chaves.port=1099

# --- Descoberta de líder (pub/sub com o Banco Central) ---
descoberta.enabled=true
descoberta.port=9001
# Host onde o Banco Central alcança o serviço de descoberta desta réplica.
descoberta.host=127.0.0.1
# O endereço publicado ao BC = descoberta.host:descoberta.port (segue o host acima).
# Banco Central onde esta instituição se registra (modelo híbrido).
bc.host=127.0.0.1
bc.port=1200

# --- Logs (apresentação): destaca o fluxo da aplicação e silencia ruído do Ratis ---
logging.level.instituicao=INFO
logging.level.raft=INFO
# ERROR: esconde o ruído interno do Ratis; nossos logs [RAFT]/[applyTransaction] narram o consenso.
logging.level.org.apache.ratis=ERROR
# Console enxuto: hora, nível, classe curta e mensagem.
logging.pattern.console=%clr(%d{HH:mm:ss.SSS}){faint} %clr(%-5level) %clr(%logger{0}){cyan} %m%n
